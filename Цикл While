# Исходные данные
my_list = [42, 69, 322, 13, 0, 99, -5, 9, 8, 7, -6, 5]

# Переменная для индекса
index = 0

# Цикл while для перебора элементов списка
while index < len(my_list):
    # Текущий элемент списка
    number = my_list[index]
    
    # Если число отрицательное, прерываем цикл
    if number < 0:
        break
    
    # Если число равно 0, пропускаем итерацию
    if number == 0:
        index += 1
        continue
    
    # Вывод положительного числа
    print(number)
    
    # Увеличиваем индекс
    index += 1
